{
  "copilot_action_name": "LINUX-DETECT-SSH-CONFIG-RISKS-COPILOT-ACTION",
  "description": "Detects risky SSH configuration and authorization files across the system. Flags world-writable SSH config or authorized_keys files inside user home directories and identifies hidden SSH directories or files outside expected locations. Produces structured NDJSON output including file paths and issues found. Writes results atomically to the Active Response log with .new fallback if locked.",
  "technology": "Linux",
  "icon": "linux",
  "script_parameters": [
    {
      "name": "ARLog",
      "type": "string",
      "required": false,
      "description": "Active Response NDJSON output path (atomic overwrite; falls back to .new if locked).",
      "default": "/var/ossec/active-response/active-responses.log"
    },
    {
      "name": "LogPath",
      "type": "string",
      "required": false,
      "description": "Path to the rotating human-readable script log.",
      "default": "/tmp/Detect-SSHConfig-Risks.log"
    },
    {
      "name": "LogMaxKB",
      "type": "integer",
      "required": false,
      "description": "Maximum size of the log file (KB) before rotation.",
      "default": 100
    },
    {
      "name": "LogKeep",
      "type": "integer",
      "required": false,
      "description": "Number of rotated log files to keep.",
      "default": 5
    }
  ],
  "repo_url": "https://raw.githubusercontent.com/socfortress/LINUX-DETECT-SSH-CONFIG-RISKS-COPILOT-ACTION/refs/heads/main",
  "script_name": "Detect-SSHConfig-Risks.sh",
  "version": "1.0.0",
  "last_updated": "2025-08-23T00:00:00Z"
}
